apiVersion: v1
kind: Service
metadata:
  name: mongodb
  namespace: default
spec:
  type: NodePort
  ports:
    - name: mongo
      port: 27017
      targetPort: 27017
      nodePort: 30017
      protocol: TCP
  selector:
    app: mongodb