syntax = "proto3";

package infraboard.mpaas.trigger;
option go_package = "github.com/infraboard/mpaas/apps/trigger";

import "apps/trigger/pb/event.proto";

service RPC {
    // 处理Gitlab的事件
    rpc HandleServiceEvent(Event) returns(Event);
}